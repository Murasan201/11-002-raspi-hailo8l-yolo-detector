# Raspberry Pi Hailo-8L USB Webcam YOLO Detector Dependencies
# Python 3.11+ required

# Computer Vision and Image Processing
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=9.5.0

# GStreamer Python Bindings (for USB Webcam pipeline)
PyGObject>=3.44.0

# Hailo AI Framework
# Note: HailoRT SDK must be installed separately following official instructions
# hailort>=4.15.0  # Install via official Hailo SDK

# Command Line Interface
argparse  # Built-in module

# Threading and Queue
queue  # Built-in module
threading  # Built-in module

# Logging and Data Handling
pandas>=2.0.0  # For CSV logging

# System packages that need to be installed via apt:
# sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# sudo apt install libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev
# sudo apt install gstreamer1.0-plugins-base gstreamer1.0-plugins-good
# sudo apt install gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly
# sudo apt install gstreamer1.0-libav gstreamer1.0-tools